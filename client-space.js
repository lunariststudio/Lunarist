// Lunarist Client Space — dedicated user/client area.
(function(){
  if(typeof window==='undefined') return;
  const boot=()=>{
    if(window.__lunaristClientSpacePatched) return true;
    if(typeof window.render!=='function'||typeof window.goRoute!=='function'||typeof window.pathForRoute!=='function'||typeof window.routeFromPath!=='function') return false;
    const originalRender=window.render, originalGoRoute=window.goRoute, originalPath=window.pathForRoute, originalRoute=window.routeFromPath;
    const css=document.createElement('style');css.id='lunarist-client-space-css';css.textContent=`.client-space-tabs{display:flex;gap:8px;overflow:auto;margin-bottom:20px;padding-bottom:3px}.client-space-tabs .filter{white-space:nowrap}.client-space-section{display:none}.client-space-section.active{display:block}.client-profile-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.client-review-card{padding:16px;border:1px solid var(--line);border-radius:16px;background:rgba(255,255,255,.025)}.client-review-stars{color:var(--gold);font-size:18px;letter-spacing:1px}.client-public-profile{max-width:900px;margin:0 auto}.client-public-card{display:grid;grid-template-columns:140px 1fr;gap:28px;align-items:start}.client-public-avatar{width:140px;height:140px;border-radius:50%;object-fit:cover;border:1px solid var(--line)}.client-public-username{color:var(--moon);font-weight:700}.client-public-url{display:inline-flex;align-items:center;gap:8px;margin-top:12px;padding:9px 12px;border:1px solid var(--line);border-radius:10px;color:var(--muted);font-size:12px;word-break:break-all}.client-website-box{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:12px 14px;margin-top:14px;border:1px solid var(--line);border-radius:12px;background:rgba(255,255,255,.025)}@media(max-width:720px){.client-profile-grid{grid-template-columns:1fr}.client-public-card{grid-template-columns:1fr;text-align:center}.client-public-avatar{margin:0 auto}.client-public-url{justify-content:center}}
`;document.head.appendChild(css);
    const isClient=()=>!!state.currentMember&&!state.currentMember.is_admin&&state.currentMember.account_type!=='member';
    const clientByUsername=u=>{u=String(u||'').replace(/^@/,'').toLowerCase();return data.members.find(m=>!m.is_admin&&m.account_type!=='member'&&String(m.username||'').toLowerCase()===u)||null};
    const clientUrl=m=>m?.username?`${location.origin}/${encodeURIComponent(m.username)}`:'';
    function addNav(){const nav=document.getElementById('navlinks');if(!nav||document.getElementById('navClientSpaceBtn'))return;const b=document.createElement('button');b.className='navbtn';b.id='navClientSpaceBtn';b.dataset.clientRoute='clients';b.textContent=state.language==='ja'?'クライアントスペース':'Client Space';b.onclick=()=>openClientSpace('overview');const admin=document.getElementById('navAdminBtn');nav.insertBefore(b,admin||null)}
    function clientShell(){const m=state.currentMember||{};const url=clientUrl(m);return `<div class="container"><section class="section" style="padding-top:55px"><div class="sectionhead"><div><div class="eyebrow">Client Space</div><h1 style="font-size:48px;margin:4px 0">${esc(m.name||'Client')} · Client Space</h1><p>Customize your profile, keep saved projects, track commissions, and manage your client reviews.</p></div></div>${url?`<div class="client-website-box"><span><b>Your Client Space website</b><br><span class="meta">${esc(url)}</span></span><button class="btn" id="copyClientUrl">Copy link</button></div>`:''}<div class="client-space-tabs" id="clientSpaceTabs"><button class="filter active" data-client-tab="overview">Overview</button><button class="filter" data-client-tab="saved">🔖 Saved</button><button class="filter" data-client-tab="commissions">My Commissions</button><button class="filter" data-client-tab="reviews">Client Reviews</button><button class="filter" data-client-tab="profile">Profile</button></div><section class="client-space-section active" id="client-space-overview"></section><section class="client-space-section" id="client-space-saved"></section><section class="client-space-section" id="client-space-commissions"></section><section class="client-space-section" id="client-space-reviews"></section><section class="client-space-section" id="client-space-profile"></section></section></div>`}
    async function getClientData(){if(!state.currentUser)return {commissions:[],reviews:[]};const [c,r]=await Promise.all([supabaseClient.from('commissions').select('id,artist_id,status,total_amount,amount,currency,target_deadline,created_at,project_title,service:service_id(id,title,thumbnail_url),artist:artist_id(id,display_name,username,avatar_url),client_reviews(id,rating,review,published,created_at)').eq('client_id',state.currentUser.id).order('created_at',{ascending:false}),supabaseClient.from('client_reviews').select('id,commission_id,artist_id,rating,review,published,created_at,artist:artist_id(id,display_name,username,avatar_url),commission:commission_id(id,project_title,status,service:service_id(title))').eq('client_id',state.currentUser.id).order('created_at',{ascending:false})]);return {commissions:c.data||[],reviews:r.data||[]}}
    function tab(name){const valid=['overview','saved','commissions','reviews','profile'];if(!valid.includes(name))name='overview';document.querySelectorAll('[data-client-tab]').forEach(b=>b.classList.toggle('active',b.dataset.clientTab===name));document.querySelectorAll('.client-space-section').forEach(s=>s.classList.toggle('active',s.id==='client-space-'+name));window.__lunaristClientTab=name;if(name==='saved')renderSaved();if(name==='commissions')renderCommissions();if(name==='reviews')renderReviews();if(name==='profile')renderProfile()}
    function renderOverview(){const el=document.getElementById('client-space-overview');if(!el)return;const m=state.currentMember||{};const d=window.__lunaristClientData||{commissions:[],reviews:[]};const url=clientUrl(m);el.innerHTML=`<div class="panel"><div class="eyebrow">Your Lunarist</div><div class="row" style="align-items:flex-start;gap:16px"><img class="avatar" style="width:72px;height:72px" src="${esc(m.avatar||'')}" onerror="handleImageError(this)"><div class="grow"><h2 style="margin:0 0 4px">Welcome back, ${esc(m.name||'Client')}.</h2><div class="meta">@${esc(m.username||'client')}</div><p class="meta">This is your personal Client Space. Publishing Projects and Services stays in Member Space.</p>${url?`<div class="client-public-url">${esc(url)}</div>`:''}<div class="stats"><span class="stat">${state.savedIds.size} saved</span><span class="stat">${d.commissions.length} commissions</span><span class="stat">${d.reviews.length} reviews</span></div><button class="btn primary" id="clientCustomizeBtn">Customize profile</button></div></div></div>`;document.getElementById('clientCustomizeBtn').onclick=()=>tab('profile')}
    function renderSaved(){const el=document.getElementById('client-space-saved');if(!el)return;const saved=data.projects.filter(p=>state.savedIds.has(p.id));el.innerHTML=`<div class="panel"><div class="eyebrow">Saved</div><h2 style="margin:4px 0">Your saved projects</h2><p class="meta">Your bookmarks are synced to your Lunarist account.</p><div class="grid" style="margin-top:16px">${saved.length?saved.map(p=>card(p)).join(''):emptyState('Nothing saved yet.','Tap 🔖 Save on any project to bookmark it here.')}</div></div>`;el.querySelectorAll('[data-project]').forEach(x=>x.onclick=()=>openProject(x.dataset.project))}
    async function renderCommissions(){const el=document.getElementById('client-space-commissions');if(!el)return;el.innerHTML='<div class="panel"><div class="meta">Loading your commissions…</div></div>';const d=window.__lunaristClientData||await getClientData();window.__lunaristClientData=d;if(!d.commissions.length){el.innerHTML=`<div class="panel">${emptyState('No commissions yet.','When you commission a Lunarist artist, your orders and completed work will appear here.')}</div>`;return}el.innerHTML=`<div class="panel"><div class="eyebrow">Client Dashboard</div><h2 style="margin:4px 0">My Commissions</h2><p class="meta">Track orders, approve delivered work, and review completed commissions.</p><div style="display:flex;flex-direction:column;gap:12px;margin-top:16px">${d.commissions.map(c=>{const a=c.artist||{},s=c.service||{},r=Array.isArray(c.client_reviews)?c.client_reviews[0]:null;const approve=c.status==='delivered',review=c.status==='completed'&&!r;return `<article class="panel" style="padding:16px;background:rgba(255,255,255,.02)"><div class="row" style="justify-content:space-between;align-items:flex-start;gap:12px"><div class="grow"><div class="eyebrow">Commission</div><h3 style="margin:3px 0 6px">${esc(s.title||c.project_title||'Commission')}</h3><div class="meta artist"><img class="avatar" style="width:26px;height:26px" src="${esc(a.avatar_url||'https://i.pravatar.cc/160?u='+a.id)}" onerror="handleImageError(this)"><span>${esc(a.display_name||a.username||'Artist')}</span></div></div><span class="pill">${esc(typeof commissionClientStatusLabel==='function'?commissionClientStatusLabel(c.status):c.status||'waitlist')}</span></div><div class="stats"><span class="stat">${esc(c.currency||'USD')} ${Number(c.total_amount||c.amount||0).toFixed(2)}</span>${c.target_deadline?`<span class="stat">Due ${esc(c.target_deadline)}</span>`:''}<span class="stat">${new Date(c.created_at).toLocaleDateString()}</span></div>${approve?`<button class="btn primary" data-client-approve="${c.id}">✓ Approve delivery &amp; complete</button>`:''}${review?`<button class="btn pink" style="margin-left:8px" data-client-review="${c.id}">Leave review</button>`:''}${r?`<div class="meta" style="margin-top:10px">✓ Review submitted</div>`:''}</article>`}).join('')}</div></div>`;el.querySelectorAll('[data-client-approve]').forEach(b=>b.onclick=async()=>{b.disabled=true;const r=await supabaseClient.from('commissions').update({status:'completed',updated_at:new Date().toISOString()}).eq('id',b.dataset.clientApprove).eq('client_id',state.currentUser.id).eq('status','delivered');if(r.error){toast(r.error.message);b.disabled=false;return}toast('Commission completed.');await refreshClientSpace('commissions')});el.querySelectorAll('[data-client-review]').forEach(b=>b.onclick=()=>{tab('reviews');setTimeout(()=>startReview(b.dataset.clientReview),0)})}
    function renderReviews(){const el=document.getElementById('client-space-reviews');if(!el)return;const d=window.__lunaristClientData||{commissions:[],reviews:[]};const pending=d.commissions.filter(c=>c.status==='completed'&&(!Array.isArray(c.client_reviews)||!c.client_reviews.length));el.innerHTML=`<div class="panel"><div class="eyebrow">Client Reviews</div><h2 style="margin:4px 0">Your reviews</h2><p class="meta">Reviews you have submitted to artists are collected here.</p>${d.reviews.length?`<div style="display:flex;flex-direction:column;gap:12px;margin-top:16px">${d.reviews.map(r=>{const a=r.artist||{},s=r.commission?.service?.title||r.commission?.project_title||'Commission';return `<div class="client-review-card"><div class="row"><img class="avatar" style="width:38px;height:38px" src="${esc(a.avatar_url||'https://i.pravatar.cc/160?u='+a.id)}" onerror="handleImageError(this)"><div class="grow"><b>${esc(a.display_name||a.username||'Artist')}</b><div class="meta">${esc(s)} · ${new Date(r.created_at).toLocaleDateString()}</div></div><span class="client-review-stars">${'★'.repeat(Number(r.rating||0))}${'☆'.repeat(5-Number(r.rating||0))}</span></div><p style="margin:10px 0 0;color:var(--muted)">${esc(r.review)}</p></div>`}).join('')}</div>`:'<div class="empty" style="margin-top:16px">You have not published any client reviews yet.</div>'}${pending.length?`<div style="margin-top:20px"><div class="eyebrow">Needs your review</div>${pending.map(c=>`<div class="client-review-card" style="margin-top:8px"><b>${esc(c.service?.title||c.project_title||'Completed commission')}</b><div class="meta">Completed commission · ${esc(c.artist?.display_name||c.artist?.username||'Artist')}</div><button class="btn pink" style="margin-top:10px" data-start-review="${c.id}">Leave review</button></div>`).join('')}</div>`:''}</div>`;el.querySelectorAll('[data-start-review]').forEach(b=>b.onclick=()=>startReview(b.dataset.startReview))}
    function startReview(id){const d=window.__lunaristClientData||{};const c=d.commissions?.find(x=>x.id===id);if(!c)return;const el=document.getElementById('client-space-reviews');const host=el?.querySelector('.panel');if(!host)return;const a=c.artist||{};const box=document.createElement('div');box.className='client-review-card';box.style.marginTop='16px';box.innerHTML='<div class="eyebrow">Review your completed commission</div><div class="field" style="margin-top:8px"><label>Rating</label><select id="clientReviewRating"><option value="5">★★★★★ — Excellent</option><option value="4">★★★★☆ — Great</option><option value="3">★★★☆☆ — Good</option><option value="2">★★☆☆☆ — Needs work</option><option value="1">★☆☆☆☆ — Poor</option></select></div><div class="field" style="margin-top:8px"><label>Your review</label><textarea id="clientReviewText" placeholder="Tell future clients about your experience…"></textarea></div><div class="heroactions"><button class="btn pink" id="clientReviewSubmit">Publish review</button><button class="btn" id="clientReviewCancel">Cancel</button></div>';host.appendChild(box);document.getElementById('clientReviewSubmit').onclick=async()=>{const text=document.getElementById('clientReviewText').value.trim(),rating=Number(document.getElementById('clientReviewRating').value||5);if(text.length<10){toast('Please write at least 10 characters.');return}const b=document.getElementById('clientReviewSubmit');b.disabled=true;const r=await supabaseClient.from('client_reviews').insert({commission_id:id,client_id:state.currentUser.id,artist_id:a.id,rating,review:text});if(r.error){toast(r.error.message);b.disabled=false;return}toast('Thank you — your review is published.');await refreshClientSpace('reviews')};document.getElementById('clientReviewCancel').onclick=()=>box.remove()}
    function renderProfile(){const el=document.getElementById('client-space-profile');if(!el)return;const m=state.currentMember||{};const url=clientUrl(m);el.innerHTML=`<div class="panel"><div class="eyebrow">Edit Client Profile</div><h2 style="margin:4px 0">Customize your profile</h2><p class="meta">Your client profile is stored in Supabase and is separate from member publishing controls.</p><div class="row" style="margin:14px 0 18px"><img class="avatar" style="width:72px;height:72px" id="clientPfAvatarPreview" src="${esc(m.avatar||'')}" onerror="handleImageError(this)"><div><button class="btn" id="clientPfAvatarBtn">Change photo</button><input type="file" id="clientPfAvatarFile" accept="image/*" style="display:none"><div class="meta" id="clientPfAvatarStatus">JPG or PNG, square works best.</div></div></div><div class="client-profile-grid"><div class="field"><label>Display name</label><input id="clientPfName" value="${esc(m.name||'')}"></div><div class="field"><label>Username</label><input id="clientPfUsername" value="${esc(m.username||'')}" maxlength="32"><div class="meta" style="margin-top:5px">Your public page: ${url?`<a href="${esc(url)}" target="_self">${esc(url)}</a>`:'save a username to create your page'}</div></div><div class="field full"><label>Bio</label><textarea id="clientPfBio">${esc(m.bio||'')}</textarea></div><div class="field full"><label>Profile Color Scheme</label><div class="theme-picker-grid">${['moonlight','cyberpink','goldember','emeraldglow','midnight'].map(t=>`<button type="button" class="theme-option ${m.theme===t?'selected':''}" data-client-theme="${t}">${t}</button>`).join('')}</div></div></div><div class="heroactions"><button class="btn primary" id="clientSaveProfile">Save profile</button><button class="btn" id="clientSignOut">Sign out</button></div><p class="meta" id="clientProfileMsg"></p></div>`;document.querySelectorAll('[data-client-theme]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-client-theme]').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');applyTheme(b.dataset.clientTheme)});document.getElementById('clientPfAvatarBtn').onclick=()=>document.getElementById('clientPfAvatarFile').click();document.getElementById('clientPfAvatarFile').onchange=async e=>{const f=e.target.files?.[0];if(!f)return;if(!f.type.startsWith('image/')){toast('Please choose an image file');return}if(f.size>5*1024*1024){toast('Image must be under 5MB');return}const s=document.getElementById('clientPfAvatarStatus');s.textContent='Uploading…';try{const u=await uploadProjectFile(f,'avatar');document.getElementById('clientPfAvatarPreview').src=u;e.target.dataset.uploadedUrl=u;s.textContent='New photo ready — click Save profile to apply.'}catch(err){s.textContent='Upload failed.';toast(err.message||'Could not upload photo')}};document.getElementById('clientSaveProfile').onclick=async()=>{const username=document.getElementById('clientPfUsername').value.trim().toLowerCase().replace(/[^a-z0-9_]/g,'').slice(0,32);if(!username){toast('Username cannot be empty');return}const selected=document.querySelector('[data-client-theme].selected')?.dataset.clientTheme||m.theme||'moonlight';const file=document.getElementById('clientPfAvatarFile');const payload={display_name:document.getElementById('clientPfName').value.trim(),username,bio:document.getElementById('clientPfBio').value.trim(),theme:selected,updated_at:new Date().toISOString()};if(file?.dataset.uploadedUrl)payload.avatar_url=file.dataset.uploadedUrl;const r=await supabaseClient.from('profiles').update(payload).eq('id',state.currentUser.id).select().single();if(r.error){toast(r.error.code==='23505'?'That username is already taken.':r.error.message);return}await refreshUser();toast('Client profile saved');addNav();renderClientSpacePage();tab('profile')};document.getElementById('clientSignOut').onclick=async()=>{await supabaseClient.auth.signOut();state.currentUser=null;state.currentMember=null;state.savedIds=new Set();toast('Signed out');goRoute('home')}}
    async function refreshClientSpace(active='overview'){if(!state.currentUser){openAuth('signin');return}await refreshUser();if(!isClient()){toast('Client Space is available for User/Client accounts.');return}await loadSavedIds();window.__lunaristClientData=await getClientData();renderClientSpacePage();tab(active)}
    function renderClientSpacePage(){const v=document.getElementById('view');if(!v)return;v.innerHTML=clientShell();document.querySelectorAll('[data-client-tab]').forEach(b=>b.onclick=()=>tab(b.dataset.clientTab));renderOverview();addNav();const copy=document.getElementById('copyClientUrl');if(copy)copy.onclick=async()=>{const u=clientUrl(state.currentMember);try{await navigator.clipboard.writeText(u);toast('Client Space link copied.')}catch(e){toast(u)}};document.querySelectorAll('.navbtn[data-route]').forEach(b=>b.classList.remove('active'));document.getElementById('navClientSpaceBtn')?.classList.add('active')}
    async function fetchPublicClient(username){if(!supabaseClient)return null;const clean=String(username||'').replace(/^@/,'').toLowerCase().replace(/[^a-z0-9_]/g,'').slice(0,32);if(!clean)return null;const {data:m,error}=await supabaseClient.from('profiles').select('id,display_name,username,bio,avatar_url,theme,account_type,is_admin').eq('username',clean).maybeSingle();if(error||!m||m.is_admin||m.account_type==='member')return null;return m}
    async function renderPublicClientProfile(target){const v=document.getElementById('view');if(!v)return;v.innerHTML='<div class="container"><section class="section" style="padding-top:70px"><div class="panel"><div class="meta">Loading client profile…</div></div></section></div>';let m=typeof target==='object'&&target?target:null;if(!m){m=await fetchPublicClient(target)}if(!m){v.innerHTML=`<div class="container"><section class="section" style="padding-top:80px"><div class="empty"><strong>Client profile not found.</strong><span>This public username is unavailable or belongs to a Lunarist Member.</span><div class="heroactions" style="justify-content:center"><button class="btn primary" id="publicClientHomeBtn">Back home</button></div></div></section></div>`;document.getElementById('publicClientHomeBtn')?.addEventListener('click',()=>originalGoRoute('home'));return}applyTheme(m.theme||'moonlight');const url=clientUrl(m);updateSeoMeta(`${m.display_name||m.username||'Client'} · Client Space`,m.bio||'Lunarist client profile',m.avatar_url||'');v.innerHTML=`<div class="container"><section class="section client-public-profile" style="padding-top:70px"><div class="client-public-card panel"><img class="client-public-avatar" src="${esc(m.avatar_url||'')}" onerror="handleImageError(this)" alt="${esc(m.display_name||'Client')}"><div><div class="eyebrow">Lunarist Client</div><h1 style="font-size:48px;margin:4px 0 6px">${esc(m.display_name||'Client')}</h1><div class="client-public-username">@${esc(m.username||'client')}</div>${m.bio?`<p style="margin-top:18px;white-space:pre-wrap;color:var(--muted)">${esc(m.bio)}</p>`:'<p class="meta" style="margin-top:18px">Lunarist client profile.</p>'}<div class="client-public-url">${esc(url)}</div><div class="heroactions" style="margin-top:18px"><button class="btn primary" id="publicClientSpaceBtn">Client Space</button><button class="btn" id="publicClientBackBtn">Discover</button></div></div></div></section></div>`;document.getElementById('publicClientSpaceBtn').onclick=()=>{if(state.currentUser&&state.currentUser.id===m.id)openClientSpace('overview');else openAuth('signin')};document.getElementById('publicClientBackBtn').onclick=()=>originalGoRoute('discover')}
    function publicClientFromPath(){const s=decodeURIComponent(location.pathname.replace(/^\/+|\/+$/,''));if(!s||s.includes('/')||['discover','artists','services','commissions','admin','clients','api'].includes(s.toLowerCase()))return null;return s}
    function openClientSpace(active='overview'){if(!state.currentUser){openAuth('signin');return}state.route='clients';if(location.pathname!=='/clients')history.pushState({route:'clients'},'', '/clients');refreshClientSpace(active)}
    window.openClientSpace=openClientSpace;window.refreshClientSpace=refreshClientSpace;window.renderClientSpacePage=renderClientSpacePage;window.setClientSpaceTab=tab;window.renderPublicClientProfile=renderPublicClientProfile;
    window.pathForRoute=function(route){if(route==='clients')return '/clients';if(String(route).startsWith('clientprofile:')){const token=String(route).slice(14);const m=member(token);return m?.username?'/'+encodeURIComponent(m.username):'/'+encodeURIComponent(token)}return originalPath(route)};
    window.routeFromPath=function(){const s=decodeURIComponent(location.pathname.replace(/^\/+|\/+$/,''));if(s==='clients')return 'clients';const original=originalRoute();if(original!=='home')return original;const publicName=publicClientFromPath();return publicName?'clientprofile:'+publicName:original};
    window.goRoute=function(route,replace){if(route==='clients'){state.route='clients';if(location.pathname!=='/clients')history[replace?'replaceState':'pushState']({route:'clients'},'', '/clients');return refreshClientSpace(window.__lunaristClientTab||'overview')}if(String(route).startsWith('clientprofile:')){const token=String(route).slice(14);state.route=route;const m=clientByUsername(token)||member(token);const path=m?.username?'/'+encodeURIComponent(m.username):'/'+encodeURIComponent(token);if(location.pathname!==path)history[replace?'replaceState':'pushState']({route},'',path);return renderPublicClientProfile(m||token)}return originalGoRoute(route,replace)};
    window.render=function(preserve){if(state.route==='clients'){renderClientSpacePage();refreshClientSpace(window.__lunaristClientTab||'overview');return}if(String(state.route).startsWith('clientprofile:')){const token=String(state.route).slice(14);const m=clientByUsername(token)||member(token);renderPublicClientProfile(m||token);return}return originalRender(preserve)};
    const originalOpenDashboard=window.openDashboard;if(typeof originalOpenDashboard==='function')window.openDashboard=async function(tabName='overview'){if(isClient())return openClientSpace(tabName==='saved'?'saved':'overview');return originalOpenDashboard(tabName)};
    function cleanMemberDrawer(){const d=document.getElementById('drawer');if(!d)return;d.querySelector('[data-dash="saved"]')?.remove();if(isClient()){d.querySelector('[data-dash="projects"]')?.remove();d.querySelector('[data-dash="services"]')?.remove()}}
    const obs=new MutationObserver(cleanMemberDrawer);const drawer=document.getElementById('drawer');if(drawer)obs.observe(drawer,{childList:true,subtree:true});
    const clientVisibility=()=>{addNav();const b=document.getElementById('navClientSpaceBtn'),c=document.getElementById('navCommissionsBtn');if(b)b.style.display=isClient()?'':'none';if(c)c.style.display=isClient()?'none':'';cleanMemberDrawer()};setInterval(clientVisibility,1000);
    window.__lunaristClientSpacePatched=true;addNav();clientVisibility();return true;
  };
  let tries=0;const t=setInterval(()=>{if(boot()||++tries>120)clearInterval(t)},100);window.addEventListener('beforeunload',()=>clearInterval(t));
})();
