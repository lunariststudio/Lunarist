(function(){'use strict';if(window.__lunaristOAuthAdminUI)return;window.__lunaristOAuthAdminUI=true;const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const toast=m=>{try{window.toast?.(m)}catch{}};let booted=false;
const css=document.createElement('style');css.textContent=`.oauth-admin{margin-top:20px}.oauth-admin .oa-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:16px}.oauth-admin .oa-kicker{font:700 10px/1 ui-monospace,monospace;letter-spacing:.16em;color:var(--moon);text-transform:uppercase}.oauth-admin .oa-head h3{margin:5px 0 4px;font-size:25px}.oauth-admin .oa-head p{margin:0;color:var(--muted)}.oa-tabs{display:flex;gap:7px;overflow:auto;margin-bottom:14px}.oa-tab{border:1px solid var(--line);background:rgba(255,255,255,.035);color:var(--muted);padding:8px 12px;border-radius:999px;font-weight:750;white-space:nowrap}.oa-tab.active{color:var(--text);border-color:var(--moon);background:rgba(201,182,255,.09)}.oa-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-bottom:14px}.oa-stat{padding:13px;border:1px solid var(--line);border-radius:14px;background:rgba(255,255,255,.025)}.oa-stat b{display:block;font-size:22px}.oa-stat span{font-size:11px;color:var(--muted)}.oa-list{display:grid;gap:10px}.oa-item{padding:15px;border:1px solid var(--line);border-radius:16px;background:rgba(255,255,255,.025)}.oa-item-top{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.oa-name{font-weight:850}.oa-id{font:11px ui-monospace,monospace;color:var(--muted);word-break:break-all;margin-top:4px}.oa-badge{font-size:10px;font-weight:850;padding:5px 8px;border-radius:999px;border:1px solid var(--line);white-space:nowrap}.oa-badge.on{color:var(--green);border-color:rgba(142,224,186,.3);background:rgba(142,224,186,.07)}.oa-badge.off{color:var(--danger);border-color:rgba(255,125,142,.3);background:rgba(255,125,142,.07)}.oa-meta{display:flex;flex-wrap:wrap;gap:6px;margin-top:10px}.oa-chip{font-size:10px;color:var(--muted);border:1px solid var(--line);padding:5px 7px;border-radius:999px}.oa-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:12px}.oauth-admin,.oauth-admin *{pointer-events:auto}.oa-btn{border:1px solid var(--line);background:rgba(255,255,255,.05);color:var(--text);padding:8px 11px;border-radius:10px;font-size:12px;font-weight:800}.oa-btn.primary{background:var(--text);color:#09070d;border-color:var(--text)}.oa-btn.danger{color:var(--danger)}.oa-code{padding:12px;border:1px dashed var(--line);border-radius:12px;background:#090810;font:11px/1.6 ui-monospace,monospace;color:var(--muted);word-break:break-all}.oa-empty{padding:28px;text-align:center;color:var(--muted);border:1px dashed var(--line);border-radius:15px}.oa-modal{position:fixed;inset:0;z-index:11000;display:flex;align-items:center;justify-content:center;padding:18px;background:rgba(2,1,6,.84);backdrop-filter:blur(15px)}.oa-modalbox{width:min(640px,100%);max-height:90vh;overflow:auto;background:#100e18;border:1px solid var(--line);border-radius:22px;padding:22px;box-shadow:var(--shadow)}.oa-modalbox h3{margin:0 0 5px;font-size:24px}.oa-modalbox p{color:var(--muted)}.oa-modalbox input,.oa-modalbox textarea{width:100%;box-sizing:border-box;border:1px solid var(--line);background:#090810;color:var(--text);border-radius:11px;padding:10px 12px;margin-top:6px;outline:none}.oa-modalbox textarea{min-height:90px;resize:vertical}.oa-checks{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}.oa-check{display:flex;gap:6px;align-items:center;border:1px solid var(--line);padding:7px 9px;border-radius:999px;color:var(--muted);font-size:11px}.oa-docs{display:grid;gap:9px}.oa-doc{padding:12px;border:1px solid var(--line);border-radius:13px}.oa-doc strong{display:block;margin-bottom:4px}.oa-doc code{font:11px ui-monospace,monospace;color:var(--moon);word-break:break-all}@media(max-width:720px){.oa-grid{grid-template-columns:1fr}}`;document.head.appendChild(css);
async function auth(){let sb=null;try{if(typeof supabaseClient!=='undefined'&&supabaseClient)sb=supabaseClient}catch{}sb=sb||window.supabaseClient||window._supabase; if(!sb?.auth?.getSession)return null;try{return(await sb.auth.getSession()).data?.session||null}catch{return null}}
async function api(path,options={}){const s=await auth();if(!s?.access_token)throw Error('Administrator session not available. Please refresh and sign in again.');const r=await fetch(path,{...options,credentials:'same-origin',cache:'no-store',headers:{Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json',...(options.headers||{})}});const text=await r.text();let d=null;try{d=text?JSON.parse(text):null}catch{}if(!r.ok)throw Error(d?.error_description||d?.error||d?.message||`Request failed (${r.status}).`);return d}
function findAdminHost(){const heads=[...document.querySelectorAll('h2,h3')];const h=heads.find(x=>(x.textContent||'').trim()==='Admin Studio');return h?.closest('.section')||h?.parentElement?.parentElement||null}
function modal(){const m=document.createElement('div');m.className='oa-modal';m.innerHTML=`<div class="oa-modalbox"><button type="button" class="btn" data-close style="float:right">×</button><div class="eyebrow">LUNARIST API</div><h3>Register OAuth application</h3><p>Create a public OAuth client. Browser apps use Authorization Code + PKCE; no client secret is generated.</p><div class="field"><label>Application name</label><input id="oaName" placeholder="Example App"></div><div class="field" style="margin-top:10px"><label>Redirect URI(s) — one per line</label><textarea id="oaRedirect" placeholder="https://example.com/oauth/callback"></textarea></div><div class="field" style="margin-top:10px"><label>Scopes</label><div class="oa-checks"><label class="oa-check"><input type="checkbox" value="identity" checked> identity</label><label class="oa-check"><input type="checkbox" value="profile" checked> profile</label><label class="oa-check"><input type="checkbox" value="offline_access"> offline_access</label></div></div><div class="oa-actions"><button type="button" class="oa-btn" data-close>Cancel</button><button type="button" class="oa-btn primary" id="oaCreate">Create application</button></div></div>`;document.body.appendChild(m);m.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>m.remove());m.querySelector('#oaCreate').onclick=async()=>{const b=m.querySelector('#oaCreate');const name=m.querySelector('#oaName').value.trim();const redirect_uris=m.querySelector('#oaRedirect').value.split(/\n+/).map(x=>x.trim()).filter(Boolean);const allowed_scopes=[...m.querySelectorAll('input[type=checkbox]:checked')].map(x=>x.value);b.disabled=true;b.textContent='Creating…';try{const d=await api('/api/lunarist?resource=oauth-admin&admin_action=create-client',{method:'POST',body:JSON.stringify({action:'create-client',name,redirect_uris,allowed_scopes})});m.remove();toast('OAuth application created.');await renderClients(d?.client_id||null)}catch(e){toast(e.message);b.disabled=false;b.textContent='Create application'}}}
async function renderClients(highlight){const host=findAdminHost();if(!host)return;let panel=document.getElementById('oauthAdminPanel');if(!panel){panel=document.createElement('div');panel.id='oauthAdminPanel';panel.className='panel oauth-admin';panel.dataset.studioPanel='api-oauth';panel.style.display='block';host.appendChild(panel)}panel.innerHTML=`<div class="oa-head"><div><div class="oa-kicker">Platform integrations</div><h3>Lunarist API & OAuth</h3><p>Manage applications, permissions, tokens and third-party connections.</p></div><button type="button" class="oa-btn primary" id="oaNew">+ New application</button></div><div class="oa-tabs"><button type="button" class="oa-tab active" data-oa-tab="apps">OAuth Apps</button><button type="button" class="oa-tab" data-oa-tab="grants">Active Connections</button><button type="button" class="oa-tab" data-oa-tab="docs">API & Docs</button></div><div id="oaContent"></div>`;panel.querySelector('#oaNew').onclick=modal;panel.querySelectorAll('.oa-tab').forEach(t=>t.onclick=()=>switchTab(t.dataset.oaTab));await switchTab('apps');if(highlight)toast(`Created ${highlight}`)}
async function switchTab(tab){const content=document.querySelector('#oauthAdminPanel #oaContent');if(!content)return;document.querySelectorAll('#oauthAdminPanel .oa-tab').forEach(t=>t.classList.toggle('active',t.dataset.oaTab===tab));try{if(tab==='apps'){const rows=await api('/api/lunarist?resource=oauth-admin&admin_action=list-clients');const active=rows.filter(x=>x.active).length;content.innerHTML=`<div class="oa-grid"><div class="oa-stat"><b>${rows.length}</b><span>Registered apps</span></div><div class="oa-stat"><b>${active}</b><span>Active apps</span></div><div class="oa-stat"><b>PKCE</b><span>Authorization Code security</span></div></div><div class="oa-list">${rows.length?rows.map(client=>`<article class="oa-item"><div class="oa-item-top"><div><div class="oa-name">${esc(client.name)}</div><div class="oa-id">${esc(client.client_id)}</div></div><span class="oa-badge ${client.active?'on':'off'}">${client.active?'Active':'Disabled'}</span></div><div class="oa-meta">${(client.allowed_scopes||[]).map(s=>`<span class="oa-chip">${esc(s)}</span>`).join('')} ${(client.redirect_uris||[]).map(u=>`<span class="oa-chip">↳ ${esc(u)}</span>`).join('')}</div><div class="oa-actions"><button type="button" class="oa-btn" data-copy="${esc(client.client_id)}">Copy client ID</button><button type="button" class="oa-btn" data-toggle="${esc(client.client_id)}" data-next="${!client.active}">${client.active?'Disable':'Enable'}</button><button type="button" class="oa-btn danger" data-revoke="${esc(client.client_id)}">Revoke all tokens</button><button type="button" class="oa-btn danger" data-delete="${esc(client.client_id)}">Delete application</button></div></article>`).join(''):'<div class="oa-empty">No OAuth applications registered yet.</div>'}</div>`;content.querySelectorAll('[data-copy]').forEach(b=>b.onclick=async()=>{await navigator.clipboard.writeText(b.dataset.copy);toast('Client ID copied.')});content.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=async()=>{try{await api('/api/lunarist?resource=oauth-admin&admin_action=toggle-client',{method:'POST',body:JSON.stringify({action:'toggle-client',client_id:b.dataset.toggle,active:b.dataset.next==='true'})});await switchTab('apps')}catch(e){toast(e.message)}});content.querySelectorAll('[data-revoke]').forEach(b=>b.onclick=async()=>{if(!confirm('Revoke every OAuth token for this application?'))return;try{await api('/api/lunarist?resource=oauth-admin&admin_action=revoke-client',{method:'POST',body:JSON.stringify({action:'revoke-client',client_id:b.dataset.revoke})});toast('Tokens revoked.');await switchTab('apps')}catch(e){toast(e.message)}});content.querySelectorAll('[data-delete]').forEach(b=>b.onclick=async()=>{if(!confirm('Delete this OAuth application? This also removes its authorization codes and tokens. This cannot be undone.'))return;try{await api('/api/lunarist?resource=oauth-admin&admin_action=delete-client',{method:'POST',body:JSON.stringify({action:'delete-client',client_id:b.dataset.delete})});toast('Application deleted.');await switchTab('apps')}catch(e){toast(e.message)}})}else if(tab==='grants'){const rows=await api('/api/lunarist?resource=oauth-admin&action=grants');content.innerHTML=`<div class="oa-grid"><div class="oa-stat"><b>${rows.filter(x=>x.active).length}</b><span>Active grants</span></div><div class="oa-stat"><b>${rows.length}</b><span>Total grants</span></div><div class="oa-stat"><b>15 min</b><span>Access token lifetime</span></div></div><div class="oa-list">${rows.length?rows.map(x=>`<article class="oa-item"><div class="oa-item-top"><div><div class="oa-name">${esc(x.client_id)}</div><div class="oa-id">${esc(x.profile?.display_name||x.profile?.username||x.lunarist_user_id)}</div></div><span class="oa-badge ${x.active?'on':'off'}">${x.active?'Active':'Revoked / expired'}</span></div><div class="oa-meta"><span class="oa-chip">${esc(x.scope)}</span><span class="oa-chip">Created ${new Date(x.created_at).toLocaleString()}</span>${x.last_used_at?`<span class="oa-chip">Used ${new Date(x.last_used_at).toLocaleString()}</span>`:''}</div><div class="oa-actions">${x.active?`<button type="button" class="oa-btn danger" data-grant-revoke="${esc(x.id)}">Revoke grant</button>`:''}</div></article>`).join(''):'<div class="oa-empty">No OAuth grants have been issued.</div>'}</div>`;content.querySelectorAll('[data-grant-revoke]').forEach(b=>b.onclick=async()=>{try{await api('/api/lunarist?resource=oauth-admin&action=revoke',{method:'POST',body:JSON.stringify({id:b.dataset.grantRevoke})});toast('Grant revoked.');await switchTab('grants')}catch(e){toast(e.message)}})}else{content.innerHTML=`<div class="oa-docs"><div class="oa-doc"><strong>Authorization</strong><code>GET ${location.origin}/oauth/authorize</code></div><div class="oa-doc"><strong>Token</strong><code>POST ${location.origin}/oauth/token</code></div><div class="oa-doc"><strong>User info</strong><code>GET ${location.origin}/oauth/userinfo</code></div><div class="oa-doc"><strong>Revocation</strong><code>POST ${location.origin}/oauth/revoke</code></div><div class="oa-doc"><strong>Discovery</strong><code>GET ${location.origin}/.well-known/oauth-authorization-server</code></div><div class="oa-doc"><strong>Supported scopes</strong><code>identity · profile · offline_access</code></div><div class="oa-doc"><strong>Security</strong><p style="margin:7px 0 0;color:var(--muted)">Public/browser clients must use Authorization Code + PKCE (S256). Access tokens are short-lived; refresh tokens rotate and can be revoked by administrators.</p></div></div>`}}catch(e){content.innerHTML=`<div class="oa-empty">${esc(e.message)}</div>`}}
// Safety binding: Admin Studio contains several legacy delegated click handlers.
// Keep OAuth controls isolated so a parent handler cannot swallow their clicks.
document.addEventListener('click',function(e){
  const b=e.target?.closest?.('#oauthAdminPanel button');
  if(!b)return;
  if(b.id==='oaNew'||b.id==='oaCreate'||b.dataset.oaTab||b.dataset.copy||b.dataset.toggle||b.dataset.revoke||b.dataset.delete||b.dataset.grantRevoke||b.dataset.close){
    e.stopPropagation();
  }
},false);

function boot(){const route=document.querySelector('[data-route="admin"].active')||location.hash==='#admin';const host=findAdminHost();if(!host||!route)return;let bar=[...host.querySelectorAll('.heroactions')].find(x=>x.querySelector('[data-studio-tab]'));if(!bar)return;let tab=bar.querySelector('[data-studio-tab="api-oauth"]');if(!tab){tab=document.createElement('button');tab.className='filter';tab.dataset.studioTab='api-oauth';tab.textContent='API & OAuth';tab.onclick=()=>{bar.querySelectorAll('[data-studio-tab]').forEach(x=>x.classList.remove('active'));tab.classList.add('active');host.querySelectorAll('[data-studio-panel]').forEach(x=>x.style.display=x.dataset.studioPanel==='api-oauth'?'block':'none');renderClients()};bar.appendChild(tab)}booted=true }const obs=new MutationObserver(()=>boot());obs.observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});setInterval(boot,800);boot()})();
