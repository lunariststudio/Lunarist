Lunarist rollback: Instagram + X fetching removed.

This package is based on the uploaded "Lunarist Studio.zip" and does not include
the Instagram/X API files or social-autofill helper added during the failed
Instagram/X integration.

Keep your existing YouTube implementation and existing project features.

Push the contents of this package to the repository, replacing the current
files. Do not add api/instagram.js, api/x.js, or social-autofill.js.
