Lunarist DeepL TOS package - fixed

Deploy index.html + api/ to Vercel. Set DEEPL_API_KEY and Supabase env vars.
The public Japanese profile TOS now auto-generates when JP is selected and tos_ja is missing.
Run the included Supabase migration once if profiles.tos_ja does not exist.
