# Project YouTube Thumbnail Full-Cover Fix

Apply to **Projects** (not only Services).

- Detects YouTube thumbnails automatically (`ytimg.com` / `youtube`).
- Forces only YouTube thumbnails to `object-fit: cover`.
- Keeps uploaded images using the auto-ratio system.
- Removes the black bars while preserving 16:9.
